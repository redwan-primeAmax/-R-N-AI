# Supabase Configuration
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google OAuth Configuration
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional: Only set if you want to override the dynamic redirect URI (must end with /auth/google/callback)
REDIRECT_URI=
